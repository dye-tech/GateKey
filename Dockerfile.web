FROM nginx:alpine

# Remove default config and add our server config
RUN rm /etc/nginx/conf.d/default.conf
COPY deploy/nginx.conf /etc/nginx/conf.d/gatex.conf

# Copy built frontend
COPY web/dist /usr/share/nginx/html

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
